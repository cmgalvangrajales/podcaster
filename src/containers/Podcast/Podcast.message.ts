import { defineMessages } from 'react-intl';

const prefix = 'containers.Podcast';

export default defineMessages({
  episodes: {
    id: `${prefix}.episodes`,
  },
  title: {
    id: `${prefix}.title`,
  },
  date: {
    id: `${prefix}.date`,
  },
  duration: {
    id: `${prefix}.duration`,
  },
});
