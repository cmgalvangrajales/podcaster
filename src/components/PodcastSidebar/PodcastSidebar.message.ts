import { defineMessages } from 'react-intl';

const prefix = 'containers.Podcast.podcastSidebar';

export default defineMessages({
  by: {
    id: `${prefix}.by`,
  },
  description: {
    id: `${prefix}.description`,
  },
});
